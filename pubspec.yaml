name: apewebsite
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  smooth_page_indicator: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/backgrounds/
    - assets/home_page/Education.png
    - assets/home_page/Alvdorf.png
    - assets/alverdorf_page/



    
    # Dodaj kolejne obrazki poniżej:
    # - assets/images/other_image.png

  # Przygotowane miejsce na przyszłe fonty, jeśli będziesz dodawać:
  # fonts:
  #   - family: CustomFont
  #     fonts:
  #       - asset: assets/fonts/CustomFont-Regular.ttf
